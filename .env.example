# 公開 URL（OG 画像・canonical・sitemap・llms.txt の絶対 URL 生成に使う）。
# 未設定なら Vercel のプレビュー URL → 本番ドメイン
# （https://color-follows-function.net）→ localhost の順にフォールバックする。
# 本番・プレビューでは通常設定不要。別ドメインで配信するときだけ指定する。
NEXT_PUBLIC_SITE_URL=https://color-follows-function.net
